<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MealPlannerApp.Views.JourneyPage"
             Title="Journey">
    <Grid RowDefinitions="Auto,Auto,*" Padding="20">
        <Label Text="21-Day Transformation" Style="{StaticResource PageTitle}" />
        <Frame Grid.Row="1" Style="{StaticResource CardFrame}">
            <VerticalStackLayout>
                <Label Text="{Binding Streak, StringFormat='Current Streak: {0} days'}" TextColor="{StaticResource Success}" FontAttributes="Bold" />
                <ProgressBar Progress="{Binding Progress}" />
                <Label Text="{Binding Progress, StringFormat='Overall: {0:P0}'}" />
            </VerticalStackLayout>
        </Frame>

        <CollectionView Grid.Row="2" ItemsSource="{Binding Tasks}">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Frame Style="{StaticResource CardFrame}">
                        <Grid ColumnDefinitions="Auto,*" ColumnSpacing="10">
                            <CheckBox IsChecked="{Binding IsCompleted}" CheckedChanged="OnTaskCheckedChanged" />
                            <VerticalStackLayout Grid.Column="1">
                                <Label Text="{Binding Title}" FontAttributes="Bold" />
                                <Label Text="{Binding Type, StringFormat='Type: {0}'}" FontSize="12" TextColor="{DynamicResource TextSecondary}" />
                                <Label Text="{Binding Day, StringFormat='Day {0}'}" FontSize="11" />
                            </VerticalStackLayout>
                        </Grid>
                    </Frame>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </Grid>
</ContentPage>
