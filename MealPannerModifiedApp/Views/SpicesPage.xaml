<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:models="clr-namespace:MealPannerModifiedApp.Models"
             x:Class="MealPannerModifiedApp.Views.SpicesPage"
             Title="Spices &amp; herbs">

    <Grid RowDefinitions="Auto,Auto,*" Padding="16,8" RowSpacing="10">
        <VerticalStackLayout Grid.Row="0" Spacing="8">
            <Label Text="Natural spices &amp; herbs"
                   FontSize="{DynamicResource FontSizeSection}"
                   FontAttributes="Bold"
                   TextColor="{DynamicResource TextPrimary}" />
            <Label x:Name="CatalogHint"
                   FontSize="{DynamicResource FontSizeCaption}"
                   TextColor="{DynamicResource TextSecondary}"
                   LineBreakMode="WordWrap" />
        </VerticalStackLayout>

        <SearchBar x:Name="SpiceSearch"
                   Grid.Row="1"
                   Placeholder="Search name, use (digestion, pepper, leaf…)"
                   TextChanged="OnSearchTextChanged"
                   SearchButtonPressed="OnSearchPressed"
                   BackgroundColor="{DynamicResource SurfaceCard}"
                   TextColor="{DynamicResource TextPrimary}"
                   PlaceholderColor="{DynamicResource TextSecondary}"
                   CancelButtonColor="{DynamicResource AccentBright}" />

        <CollectionView x:Name="SpiceCollection"
                        Grid.Row="2"
                        SelectionMode="Single"
                        SelectionChanged="OnSpiceSelected"
                        Margin="0,8,0,0">
            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="models:SpiceEntry">
                    <Border StrokeShape="RoundRectangle 14"
                            Stroke="{DynamicResource PanelBorder}"
                            StrokeThickness="1"
                            BackgroundColor="{DynamicResource SurfaceCard}"
                            Padding="14"
                            Margin="0,0,0,8">
                        <Grid ColumnDefinitions="*,Auto" RowDefinitions="Auto,Auto">
                            <Label Grid.Row="0" Grid.Column="0"
                                   Text="{Binding Name}"
                                   FontAttributes="Bold"
                                   FontSize="{DynamicResource FontSizeBody}"
                                   TextColor="{DynamicResource TextPrimary}"
                                   LineBreakMode="WordWrap" />
                            <Label Grid.Row="0" Grid.Column="1"
                                   Text="›"
                                   FontSize="{DynamicResource FontSizeLead}"
                                   TextColor="{DynamicResource AccentBright}"
                                   VerticalOptions="Start" />
                            <Label Grid.Row="1" Grid.ColumnSpan="2"
                                   Text="{Binding Category}"
                                   FontSize="{DynamicResource FontSizeMicro}"
                                   TextColor="{DynamicResource TextSecondary}" />
                        </Grid>
                    </Border>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </Grid>
</ContentPage>
